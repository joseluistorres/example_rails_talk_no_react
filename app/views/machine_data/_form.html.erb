<%= form_for(machine_datum) do |f| %>
  <% if machine_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(machine_datum.errors.count, "error") %> prohibited this machine_datum from being saved:</h2>

      <ul>
      <% machine_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :preread_json %>
    <%= f.text_field :preread_json %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
